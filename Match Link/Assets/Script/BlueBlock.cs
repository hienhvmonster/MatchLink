using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlueBlock : BlockType
{

    // Start is called before the first frame update
    void Start()
    {
        blockTypeID = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
